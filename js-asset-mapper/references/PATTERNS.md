# JS 资产映射关键模式

## API 关键字

`fetch`, `axios`, `request`, `http`, `service`, `ajax`, `graphql`, `gql`, `WebSocket`, `EventSource`, `signalR`, `PageMethods`, `WebServiceProxy`, `.asmx`, `.ashx`, `.svc`。

## 路由关键字

`routes`, `path`, `component`, `children`, `redirect`, `alias`, `beforeEach`, `addRoute`, `meta`, `requiresAuth`, `roles`, `permissions`, `canActivate`, `PrivateRoute`。

## 参数关键字

`params`, `query`, `data`, `body`, `FormData`, `headers`, `Authorization`, `X-Token`, `tenantId`, `orgId`, `userId`, `roleId`, `pageNum`, `pageSize`。
